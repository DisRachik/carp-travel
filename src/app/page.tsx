import { HeroSection, AboutSection, ServicesSection, CareerSection, GallerySection } from '@/components/sections';

export default function Home() {
	return (
		<main>
			<HeroSection />
			<AboutSection />
			<ServicesSection />
			<CareerSection />
			<GallerySection />
		</main>
	);
}
